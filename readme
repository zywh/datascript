loadmls.sh  

download meta and pic from windows
load meta into table and clean pic and data


#export_local.sh  export SQL and load csv into maplecity
export_condo.sh export Condo SQL and load csv into maplecity table
export_resi.sh export resi SQL

